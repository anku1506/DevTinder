In this episode we make our requuest dynamic to add data . 
app.use(express.json()); - used this express middleware
wehn we use anyfunction without route inside app.use , it applies to all the route so express.json applied to all ropute .